# Setu placeholder

Will be implemented in Phase 2 if/when we expand bank-feed coverage to
Indian operations. The folder slot is reserved so the swap procedure
stays a single env-var change.

Endpoint (for reference): `docs.setu.co` → Account Aggregator / Bills.
