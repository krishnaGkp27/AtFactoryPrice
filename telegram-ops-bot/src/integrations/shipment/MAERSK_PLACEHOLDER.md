# Maersk placeholder

Will be implemented in Phase 2 once Maersk Track-and-Trace API
credentials are obtained. The folder slot is reserved so the swap
procedure stays a single env-var change.

Endpoint (for reference): `developer.maersk.com` → Track and Trace.
